server:
  port: 8888
spring:
  datasource:
      application:
        name: dslist
jpa:
  properties:
    hibernate:
      ddl-auto: none
      jdbc:
        lob:
          non_contextual_creation: 'true'
  database-platform: org.hibernate.dialect.PostgreSQLDialect
